---
import SiteLayout from "../../layouts/SiteLayout.astro";
import { href } from "../../lib/href";
import { t } from "../../i18n/i18n";
---

<SiteLayout lang="ja" title={t("ja", "bookmarks.title")} currentPath="/bookmarks/">
  <div class="glass rounded-2xl p-5">
    <h1 class="m-0 text-2xl font-semibold leading-tight tracking-tight text-slate-950 md:text-3xl">
      {t("ja", "bookmarks.title")}
    </h1>
    <p class="mt-2 text-sm text-slate-700">
      <span id="acg-bookmarks-count">0</span> 件
    </p>
    <div class="mt-3 flex flex-wrap gap-2">
      <button
        type="button"
        id="acg-bookmarks-export"
        class="glass rounded-xl px-4 py-2 text-xs font-medium text-slate-950 clickable"
      >
        {t("ja", "bookmarks.export")}
      </button>
      <button
        type="button"
        id="acg-bookmarks-import"
        class="glass rounded-xl px-4 py-2 text-xs font-medium text-slate-950 clickable"
      >
        {t("ja", "bookmarks.import")}
      </button>
      <button
        type="button"
        id="acg-bookmarks-clear"
        class="rounded-xl border border-rose-600/20 bg-rose-500/10 px-4 py-2 text-xs font-medium text-rose-800 clickable"
      >
        {t("ja", "bookmarks.clear")}
      </button>
      <input id="acg-bookmarks-import-file" type="file" accept="application/json" class="hidden" />
    </div>
    <p class="mt-3 text-xs text-slate-600">{t("ja", "bookmarks.hint")}</p>
    <p id="acg-bookmarks-message" class="mt-2 hidden text-xs text-slate-700"></p>
    <noscript>
      <p class="mt-2 text-sm text-slate-700">このページは JavaScript が必要です（ローカルのブックマークを読むため）。</p>
    </noscript>
  </div>

  <div class="mt-6" id="acg-bookmarks" hidden>
    <div
      id="acg-bookmarks-grid"
      class="grid gap-4 sm:grid-cols-2 lg:gap-5 xl:grid-cols-3 2xl:grid-cols-4"
    ></div>
  </div>

  <div class="mt-6 glass shine rounded-2xl p-8" id="acg-bookmarks-empty">
    <div class="flex flex-col items-center gap-4 text-center">
      <div class="radar grid size-16 place-items-center shadow-sm">
        <span class="text-sm font-bold text-white/90">☆</span>
      </div>
      <div>
        <p class="m-0 text-sm font-medium text-slate-950">{t("ja", "bookmarks.empty")}</p>
        <p class="mt-2 text-xs leading-relaxed text-slate-600">気になる記事は右上の☆でブックマークできます。</p>
      </div>
      <a class="rounded-xl bg-slate-950 px-4 py-2 text-xs font-medium text-white clickable" href={href("/ja/")}>
        最新へ
      </a>
    </div>
  </div>
</SiteLayout>
